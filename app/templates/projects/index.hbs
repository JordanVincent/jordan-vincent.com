<div class="content-container" id="projects-index">

  <div class="centered-column">

    <div class="side-column">
      {{nav-menu}}
    </div>

    <article>
      <h1>Projects</h1>

      <p>Here is a list of projects I created or contributed to. My experiences range from cofounding a {{link-to 'massive multiplayed game' 'project' 2}}, designing a {{link-to 'physical product' 'project' 24}} and working in a {{link-to 'startup' 'project' 1}} in San Francisco.</p>

      <h2>Main projects</h2>
      <div class="raw clearfix">
        {{#each project in mainProjects}}
          <div class="col-xs-6">
            {{project-item project=project}}
          </div>
        {{/each}}
      </div>

      <h2>Side projects</h2>
      <div class="raw clearfix">
        {{#each project in sideProjects}}
          <div class="col-xs-6">
            {{project-item project=project}}
          </div>
        {{/each}}
      </div>

      <h2>Class projects</h2>
      <div class="raw clearfix">
        {{#each project in classProjects}}
          <div class="col-xs-6">
            {{project-item project=project}}
          </div>
        {{/each}}
      </div>
    </article>

    {{nav-inline}}

  </div>
</div>