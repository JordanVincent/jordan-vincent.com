{{#link-to 'project' project}}
  <div class="front" {{bind-attr style=style title=project.name}}></div>
  <div class="back">
    <h3>{{project.name}}</h3>
    <span class="date">{{project.date}}</span>
    <span class="summary">{{project.summary}}</span>
  </div>
{{/link-to}}